# KRYONIX Platform Environment Variables

# ===========================================
# FRONTEND (Next.js) - Vercel Configuration
# ===========================================

# Application
NODE_ENV=production
NEXT_TELEMETRY_DISABLED=1

# Backend API URL (Render deployment)
BACKEND_URL=https://kryonix-backend.onrender.com
NEXT_PUBLIC_BACKEND_URL=https://kryonix-backend.onrender.com

# Site Configuration
NEXT_PUBLIC_SITE_URL=https://kryonix-plataforma.vercel.app
NEXT_PUBLIC_APP_NAME=KRYONIX
NEXT_PUBLIC_APP_VERSION=1.0.0

# Builder.io Integration
NEXT_PUBLIC_BUILDER_IO_KEY=your_builder_io_key_here

# Analytics (Optional)
NEXT_PUBLIC_GA_ID=your_google_analytics_id
VERCEL_ANALYTICS_ID=your_vercel_analytics_id

# ===========================================
# BACKEND (Node.js) - Render Configuration  
# ===========================================

# Server Configuration
PORT=5000
NODE_ENV=production

# Database (PostgreSQL - Auto-configured by Render)
DATABASE_URL=postgresql://username:password@host:port/database
POSTGRES_URL=postgresql://username:password@host:port/database

# Security
JWT_SECRET=your_super_secret_jwt_key_here_minimum_32_characters
WEBHOOK_SECRET=your_webhook_secret_here_minimum_32_characters
BCRYPT_ROUNDS=12

# CORS Configuration
CORS_ORIGIN=https://kryonix-plataforma.vercel.app,https://www.kryonix.com.br
FRONTEND_URL=https://kryonix-plataforma.vercel.app

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=info

# Email Configuration (Optional - for notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password

# WhatsApp Integration (Optional)
WHATSAPP_API_URL=your_whatsapp_api_url
WHATSAPP_API_KEY=your_whatsapp_api_key

# ===========================================
# DEPLOYMENT INSTRUCTIONS
# ===========================================

# VERCEL (Frontend):
# 1. Connect your GitHub repository to Vercel
# 2. Set environment variables in Vercel dashboard:
#    - BACKEND_URL
#    - NEXT_PUBLIC_BACKEND_URL
#    - NEXT_PUBLIC_SITE_URL
# 3. Deploy automatically on push to main branch

# RENDER (Backend):
# 1. Create a new Web Service on Render
# 2. Connect your GitHub repository
# 3. Set root directory to: ./
# 4. Set build command to: cd backend && npm install
# 5. Set start command to: cd backend && npm start
# 6. Create PostgreSQL database service
# 7. Set environment variables in Render dashboard:
#    - JWT_SECRET (generate secure random string)
#    - WEBHOOK_SECRET (generate secure random string)
#    - CORS_ORIGIN
#    - FRONTEND_URL
# 8. DATABASE_URL will be auto-configured by Render

# ===========================================
# SECURITY CHECKLIST
# ===========================================

# ✅ Generate strong JWT_SECRET (minimum 32 characters)
# ✅ Generate strong WEBHOOK_SECRET (minimum 32 characters)  
# ✅ Set proper CORS_ORIGIN (only your domains)
# ✅ Use HTTPS in production (automatic with Vercel/Render)
# ✅ Enable PostgreSQL SSL in production
# ✅ Set proper Content Security Policy headers
# ✅ Use rate limiting for API endpoints
# ✅ Never commit secrets to version control

# ===========================================
# MONITORING & HEALTH CHECKS
# ===========================================

# Health check endpoints:
# - Frontend: https://kryonix-plataforma.vercel.app/api/health
# - Backend: https://kryonix-backend.onrender.com/health

# Status page:
# - System status: https://kryonix-backend.onrender.com/api/system/status
