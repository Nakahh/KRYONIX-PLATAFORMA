# ============================================================================
# KRYONIX PLATFORM - CONFIGURAÇÃO DE VARIÁVEIS DE AMBIENTE PARA PRODUÇÃO
# ============================================================================

# ===========================================
# FRONTEND (VERCEL) - Variáveis Públicas
# ===========================================

# Build & Runtime
NODE_ENV=production
NEXT_TELEMETRY_DISABLED=1

# Backend API (Render)
NEXT_PUBLIC_API_URL=https://kryonix-backend.onrender.com
NEXT_PUBLIC_WS_URL=wss://kryonix-backend.onrender.com

# Vercel Specific
VERCEL_ENV=production
VERCEL_REGION=gru1

# WhatsApp/Push Notifications
NEXT_PUBLIC_VAPID_KEY=your_vapid_key_here

# Analytics (opcional)
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
NEXT_PUBLIC_HOTJAR_ID=your_hotjar_id

# External Services (público)
NEXT_PUBLIC_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project

# ===========================================
# BACKEND (RENDER) - Variáveis Privadas
# ===========================================

# Application
NODE_ENV=production
PORT=8080
HOSTNAME=0.0.0.0
NEXT_TELEMETRY_DISABLED=1

# Database (Render auto-configura)
# DATABASE_URL=postgresql://user:pass@host:5432/database
# POSTGRES_URL=postgresql://user:pass@host:5432/database

# Redis (Render auto-configura)
# REDIS_URL=redis://:password@host:6379

# Security Secrets (Render auto-gera)
# JWT_SECRET=auto-generated-secret
# WEBHOOK_SECRET=auto-generated-secret
# ENCRYPTION_KEY=auto-generated-secret
# SESSION_SECRET=auto-generated-secret

# CORS Configuration
CORS_ORIGINS=https://kryonix-frontend.vercel.app,https://kryonix.com.br,https://app.kryonix.com.br

# Keycloak Authentication
KEYCLOAK_URL=https://keycloak.kryonix.com.br
KEYCLOAK_ADMIN_USERNAME=kryonix
# KEYCLOAK_ADMIN_PASSWORD=auto-generated

# MinIO Storage
MINIO_URL=https://storage.kryonix.com.br
MINIO_ACCESS_KEY=kryonix
# MINIO_SECRET_KEY=auto-generated

# WhatsApp Integration
EVOLUTION_API_URL=https://api.kryonix.com.br
EVOLUTION_API_KEY=your_evolution_api_key
EVOLUTION_INSTANCE=kryonix
WHATSAPP_WEBHOOK_URL=https://kryonix-backend.onrender.com/api/whatsapp-webhook

# Notifications
ALERT_WHATSAPP=+5517981805327
ALERT_EMAIL=monitoring@kryonix.com.br

# Email Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=contato@kryonix.com.br
SMTP_PASS=your_app_password

# External APIs
SENDGRID_API_KEY=SG.your_sendgrid_key
TWILIO_ACCOUNT_SID=ACyour_twilio_sid
TWILIO_AUTH_TOKEN=your_twilio_token

# Firebase/Push Notifications
FIREBASE_API_KEY=your_firebase_key
FIREBASE_SENDER_ID=your_sender_id
FIREBASE_APP_ID=your_app_id
FIREBASE_VAPID_KEY=your_vapid_key

# GitHub Integration
GITHUB_WEBHOOK_ENABLED=true
GITHUB_PAT=ghp_your_personal_access_token

# Features
AUTO_MIGRATE=true
AUTO_BACKUP=true
HEALTH_CHECK_ENABLED=true
MONITORING_ENABLED=true

# ===========================================
# INSTRUÇÕES DE CONFIGURAÇÃO
# ===========================================

# VERCEL (Frontend):
# 1. Execute: vercel env add NEXT_PUBLIC_API_URL production
# 2. Execute: vercel env add NODE_ENV production  
# 3. Configure no Dashboard: https://vercel.com/dashboard

# RENDER (Backend):
# 1. As variáveis DATABASE_URL e REDIS_URL são configuradas automaticamente
# 2. As variáveis com "auto-generated" são geradas automaticamente pelo Render
# 3. Configure as demais no Dashboard: https://dashboard.render.com

# SEGURANÇA:
# - Nunca commite este arquivo com valores reais
# - Use este arquivo apenas como template
# - Valores sensíveis devem ser configurados diretamente nos dashboards
